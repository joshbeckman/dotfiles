Design a JSON API for incrementally reading an append-only event stream. Clients may disconnect, events can share timestamps, and retention may delete old events. The existing endpoint accepts `after_id` and returns up to 100 events; compatibility must be preserved for one release. Specify the request, successful response, invalid/expired cursor errors, ordering rule, and migration path. Do not edit files. Keep the proposal under 300 words.
